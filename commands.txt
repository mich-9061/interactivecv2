build:
    mvn package 
    mvn clean package
run: 
    mvn spring-boot:run
    docker compose up --build <-- mostra il terminale di docker
build docker: 
    docker build -t interactivecv .
    docker compose up --build <-- builda e lancia
create container docker:
    docker run -d -p 8080:8080 interactivecv